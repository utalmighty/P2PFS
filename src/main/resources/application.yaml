spring:
  profiles:
    active: ${ACTIVE_PROFILE}
---

spring:
  profile: production
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  
server:
  port: 8080

persistCount: 10
counterId: File count
schema: P2PFS
tableName: counter

---
spring:
  profile: development
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  
server:
    port: 8080

persistCount: 10
counterId: File count
schema: P2PFS
tableName: counter